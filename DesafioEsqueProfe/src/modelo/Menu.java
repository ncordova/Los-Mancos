package modelo;

import java.awt.Point;
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

/**
 *
 * @author Alex971
 */
public class Menu {

    public Menu() {
        Figura newFig;
        Cuadrilatero cuad;
        Triangulo tri;
        List<Figura> figuras = new ArrayList<>();
        int tipo;
        Point newPunto;
        Scanner scan = new Scanner(System.in);
        

        /*
    if(tipo == 1){          //Triangulos
        
    Triangulo tri = new Triangulo(tipo+2); //revizar y optimizar
        tri.setPunto(newPunto, 1);
        tri.setPunto(newPunto, 2);
        tri.setPunto(newPunto, 3);
    
        figuras.add(
    }else if(tipo == 2){    //Cuadrado
    
    }else{                  //Otro
    
    }
         */
    }
}
